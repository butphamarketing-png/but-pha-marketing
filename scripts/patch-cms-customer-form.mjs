/**
 * @deprecated CMS UI is built from Asset-Tracker-extracted source via `npm run build:cms`.
 * This patch targeted the old committed bundle (index-b9-FGp92.js) and is no longer needed.
 */
console.error("patch-cms-customer-form.mjs is deprecated. Run: npm run build:cms");
process.exit(1);
